<%= form_with url: "/users/#{@user.id}/posts/#{@post.id}/create_comment", scope: :comment do |form| %>
<div class="comment-contain">
	<%= form.text_area :text, placeholder: "Type something here...", rows: 5, required: true %>
	<%= form.submit "Comment", class: "button" %>
  </div>
<% end %>  